<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Homepage
        style. 
         body{font-family:-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif ;}
        
    body
      h1 welcome to kurzarbeit app
      p  easily you can find a mini job

      h2 Client Detail
      p #{clients.name} 

      h3 Matching History 
          if client.matchings.length 
            ol
             each matching in client.matchings
              li From #{matching.origin} to #{matching.destination}
          else 
              p no matching